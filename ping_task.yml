---
- name: "Ping Task"
  ios_ping:
    dest: "{{ item }}"
    count: 2
    state: present 
- name: write_data
  lineinfile:
    line: "{{item}}"
    path: "outputs/{{inventory_hostname}}_ping_result.csv"
  delegate_to: localhost
